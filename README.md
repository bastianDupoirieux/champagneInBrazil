# champagneInBrazil

To run the application for now:
1. Setup backend: In root directory run

`python servers/backend/server.py --port 8000 --reload true`

2. Setup frontend: In `servers/frontend` run:
`npm run dev`